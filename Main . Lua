math.randomseed(tick())

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

local plr = Players.LocalPlayer
local cam = Workspace.CurrentCamera
local guiParent = plr:WaitForChild("PlayerGui")

local enabled = false
local target = nil

local pred = 1
local yOffset = Vector3.new(0,2,0)

local camDist = 12
local camHeight = 5

local function validChar(c)
    if not c then return false end
    local h = c:FindFirstChildOfClass("Humanoid")
    local r = c:FindFirstChild("HumanoidRootPart")
    return h and r and h.Health > 0
end

local function getTarget()
    local best, val = nil, math.huge
    local middle = Vector2.new(cam.ViewportSize.X/2, cam.ViewportSize.Y/2)

    for _,v in ipairs(Players:GetPlayers()) do
        if v ~= plr and v.Character and validChar(v.Character) then
            local hrp = v.Character.HumanoidRootPart
            local pos, onScreen = cam:WorldToViewportPoint(hrp.Position)

            if onScreen then
                local dist = (Vector2.new(pos.X,pos.Y) - middle).Magnitude
                if dist < val then
                    val = dist
                    best = hrp
                end
            end
        end
    end

    return best
end

RunService.RenderStepped:Connect(function()
    if not enabled then return end

    local char = plr.Character
    if not char then return end

    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if not target or not target.Parent then
        target = getTarget()
    end
    if not target then return end

    local vel = target.AssemblyLinearVelocity or target.Velocity
    local aimPos = target.Position + vel * pred + yOffset

    local camPos = hrp.Position - hrp.CFrame.LookVector * camDist + Vector3.new(0, camHeight, 0)
    cam.CFrame = CFrame.new(camPos, aimPos)
end)

-- UI
local gui = Instance.new("ScreenGui")
gui.Name = "TargetsakenUI"
gui.Parent = guiParent
gui.ResetOnSpawn = false

-- loading screen
local loadGui = Instance.new("ScreenGui")
loadGui.Name = "TargetsakenLoading"
loadGui.Parent = guiParent

local lf = Instance.new("Frame")
lf.Size = UDim2.new(.6,0,.6,0)
lf.Position = UDim2.new(.2,0,.2,0)
lf.BackgroundColor3 = Color3.fromRGB(0,0,0)
lf.BackgroundTransparency = .2
lf.Parent = loadGui
Instance.new("UICorner", lf)

local img = Instance.new("ImageLabel")
img.Size = UDim2.new(1,0,1,0)
img.BackgroundTransparency = 1
img.Image = "rbxassetid://92460766977717"
img.Parent = lf

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,0,0.4,0)
title.Position = UDim2.new(0,0,0.25,0)
title.BackgroundTransparency = 1
title.Text = "TARGETSAKEN"
title.TextScaled = true
title.Font = Enum.Font.SourceSansBold
title.TextColor3 = Color3.fromRGB(255,0,0)
title.Parent = lf

-- 🔥 Random subtitle
local subtitle = Instance.new("TextLabel")
subtitle.Size = UDim2.new(1,0,0.15,0)
subtitle.Position = UDim2.new(0,0,0.7,0)
subtitle.BackgroundTransparency = 1
subtitle.TextScaled = true
subtitle.Font = Enum.Font.SourceSans
subtitle.TextColor3 = Color3.fromRGB(255,255,255)
subtitle.Parent = lf

local messages = {
    "Targetsaken was made by me🔥",
    "Targetsaken targets for you🔥"
}

subtitle.Text = messages[math.random(1, #messages)]

gui.Enabled = false
task.delay(2.5,function()
    loadGui:Destroy()
    gui.Enabled = true
end)

-- main frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0,250,0,160)
frame.Position = UDim2.new(.7,0,.6,0)
frame.BackgroundColor3 = Color3.fromRGB(30,30,30)
frame.Parent = gui
Instance.new("UICorner", frame)

local title2 = Instance.new("TextLabel")
title2.Size = UDim2.new(1,0,0,30)
title2.Text = "TARGETSAKEN"
title2.TextScaled = true
title2.Font = Enum.Font.SourceSansBold
title2.TextColor3 = Color3.fromRGB(255,0,0)
title2.BackgroundTransparency = .2
title2.Parent = frame
Instance.new("UICorner", title2)

local lock = Instance.new("TextButton")
lock.Size = UDim2.new(1,-10,0,28)
lock.Position = UDim2.new(0,5,0,40)
lock.Text = "LOCK: OFF"
lock.BackgroundColor3 = Color3.fromRGB(50,50,50)
lock.TextColor3 = Color3.new(1,1,1)
lock.Parent = frame
Instance.new("UICorner", lock)

local label = Instance.new("TextLabel")
label.Size = UDim2.new(1,-10,0,20)
label.Position = UDim2.new(0,5,0,75)
label.Text = "Prediction (1-10):"
label.TextScaled = true
label.BackgroundTransparency = 1
label.TextColor3 = Color3.new(1,1,1)
label.Parent = frame

local box = Instance.new("TextBox")
box.Size = UDim2.new(1,-10,0,28)
box.Position = UDim2.new(0,5,0,100)
box.BackgroundColor3 = Color3.fromRGB(70,70,70)
box.TextColor3 = Color3.fromRGB(255,255,255)
box.PlaceholderText = tostring(pred)
box.ClearTextOnFocus = false
box.Parent = frame
Instance.new("UICorner", box)

box.FocusLost:Connect(function(enter)
    if enter then
        local v = tonumber(box.Text)
        if v and v >= 1 and v <= 10 then
            pred = v
            box.PlaceholderText = tostring(v)
        end
        box.Text = ""
    end
end)

lock.MouseButton1Click:Connect(function()
    enabled = not enabled
    lock.Text = enabled and "LOCK: ON" or "LOCK: OFF"
    if not enabled then target = nil end
end)

-- draggable
local function drag(obj)
    local d,inp,start,pos

    obj.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            d = true
            start = i.Position
            pos = obj.Position
            i.Changed:Connect(function()
                if i.UserInputState == Enum.UserInputState.End then d = false end
            end)
        end
    end)

    obj.InputChanged:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch then
            inp = i
        end
    end)

    UIS.InputChanged:Connect(function(i)
        if i == inp and d then
            local delta = i.Position - start
            obj.Position = UDim2.new(pos.X.Scale,pos.X.Offset+delta.X,pos.Y.Scale,pos.Y.Offset+delta.Y)
        end
    end)
end

drag(frame)
